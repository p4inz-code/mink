pub fn add(a: Int, b: Int) -> Int { return a + b; }
pub fn multiply(a: Int, b: Int) -> Int { return a * b; }
fn private_helper() -> Int { return 42; }
pub struct Point { x: Int, y: Int }
pub enum Color { Red, Green, Blue }
